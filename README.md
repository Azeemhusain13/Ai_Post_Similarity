This project implements an AI-powered system to identify similar social media posts and group them under common topics from a large Excel dataset.

The system is designed to work with multilingual text (English, Hindi, Hinglish) and is optimized for large datasets using a hybrid retrieval pipeline.

A Streamlit interface allows users to upload Excel files dynamically and analyze similarity between posts.
